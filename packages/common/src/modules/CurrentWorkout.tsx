import * as React from "react";
import { Text, View } from "react-native";

interface Props {}

export const CurrentWorkout: React.FC<Props> = () => {
    return (
        <View>
            <Text>CurrentWorkout page</Text>
        </View>
    );
};
